


<script type="text/javascript">
    $(function () {
        //lấy dinh dưỡng món ăn
        //$("a.lnkDDMonAn").hover(function () {
        //    $("#tpDDMonAn").dialog({
        //        autoOpen: false,
        //        title: 'Thành phần finh dưỡng',
        //        modal: true,
        //        width: 800,
        //        dialogClass: 'mod-no-close',
        //        open: function (event, ui) {
        //            $("#tpDDMonAn").show();
        //        },
        //        buttons: {
        //            "Đóng": function () {
        //                $(this).dialog("close");
        //            },
        //        }
        //    });
        //    $("#openModal").dialog("open");
        //});
        //hiển thị popup lấy chi tiết thành phần món ăn
        @*$("a.lnkChiTietTP").on("click", function () {
            var myModal = $('#openModal');
            var id = $(this).data('value');//lấy giá trị id của bản ghi
            console.log(id);
            //lấy hàng trong bảng dữ liệu
            var $tr = $(this).closest('tr');
            //lấy thông tin chi tiết thành phần thực phẩm trong món ăn
            $("#openModal").dialog({
                autoOpen: false,
                title: 'Thành phần món ăn',
                modal: true,
                width: 800,
                dialogClass: 'mod-no-close',
                open: function (event, ui) {
                    $(this).load("@Url.Action("TPMonAnPartial","MonAn")", { maMonAn: id });
                    @*$.ajax({
                        type: "POST",
                        url: "@(Url.Action("TPMonAnPartial", "MonAn"))",
                        data: {maMonAn:id},
                        success: function (data) {
                            //Load the CreateAlbumPartial action which will return
                            // the partial view _CreateAlbumPartial
                            $(this).html(data);
                            //$(this).load("Url.Action("TPMonAnPartial","MonAn")");
                        }
                    });
                },
                buttons: {
                    "Đóng": function () {
                        $(this).dialog("close");
                    },
                    "Chỉnh sửa": function () {
                        window.location.href = "/Manager/MonAn/Edit?type=Edit&mamonan=" + id;
                    }
                }
            });
            $("#openModal").dialog("open");
        });/////edn modal*@
    });
</script>
