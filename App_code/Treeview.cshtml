@helper GetTreeView(List<TestDA.Areas.Manager.Models.ViewModel.NhomTinData> siteMenu, int? parentID)
{
    foreach (var i in siteMenu.Where(a => a.danhMucCha.Equals(parentID)))
    {
        <li>
            @{var submenu = siteMenu.Where(a => a.danhMucCha.Equals(i.maNhom)).Count();}
            @* @if (submenu > 0)
                {
                    <span class="collapsible">&nbsp;</span>
                }
                else
                {
                    <span style="width:15px; display:inline-block">&nbsp;</span>
                }*@
            <a href="~/News/GroupNews?danhMuc=@i.maNhom">@i.tenNhom</a>
            @if (submenu > 0)
            {
                <ul class="sub-menu">
                    @Treeview.GetTreeView(siteMenu, i.maNhom)
                    @* Recursive  Call for Populate Sub items here*@
                </ul>
            }
        </li>
    }
}


